version: "3.7"

services:

    app:
        build: ./App
        container_name: App
        restart: always
        environment: 
            - APP_NAME=CordLte
        expose: 
            - 8080
    nginx:
        build: ./Nginx
        container_name: Nginx
        restart: always
        ports:
            - "80:80"
            

